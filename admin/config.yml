backend:
  name: github
  repo: vladionolteanu-star/pensiuneaalexandra
  branch: main
  base_url: https://www.pensiuneaalexandra.com
  auth_endpoint: api/auth

media_folder: "images/rooms"
public_folder: "/images/rooms"

collections:
  - name: "rooms"
    label: "🛏️ Camere — Poze & Prețuri"
    label_singular: "Cameră"
    description: "Gestionează pozele și prețurile pentru fiecare cameră"
    files:
      - name: "dubla-clasic"
        label: "Camera Dublă Clasic"
        file: "content/rooms/dubla-clasic.json"
        fields:
          - { name: "name", label: "Nume cameră", widget: "string", default: "Camera Dublă Clasic" }
          - { name: "price", label: "Preț per noapte (RON)", widget: "number", value_type: "int", min: 50, max: 5000, default: 250 }
          - name: "photos"
            label: "Poze (max 20)"
            widget: "list"
            max: 20
            summary: "{{fields.alt}}"
            fields:
              - { name: "src", label: "Imagine", widget: "image" }
              - { name: "alt", label: "Descriere", widget: "string", default: "Camera Dublă Clasic" }

      - name: "twin"
        label: "Camera Twin"
        file: "content/rooms/twin.json"
        fields:
          - { name: "name", label: "Nume cameră", widget: "string", default: "Camera Twin" }
          - { name: "price", label: "Preț per noapte (RON)", widget: "number", value_type: "int", min: 50, max: 5000, default: 230 }
          - name: "photos"
            label: "Poze (max 20)"
            widget: "list"
            max: 20
            summary: "{{fields.alt}}"
            fields:
              - { name: "src", label: "Imagine", widget: "image" }
              - { name: "alt", label: "Descriere", widget: "string", default: "Camera Twin" }

      - name: "familie"
        label: "Camera Familie"
        file: "content/rooms/familie.json"
        fields:
          - { name: "name", label: "Nume cameră", widget: "string", default: "Camera Familie" }
          - { name: "price", label: "Preț per noapte (RON)", widget: "number", value_type: "int", min: 50, max: 5000, default: 350 }
          - name: "photos"
            label: "Poze (max 20)"
            widget: "list"
            max: 20
            summary: "{{fields.alt}}"
            fields:
              - { name: "src", label: "Imagine", widget: "image" }
              - { name: "alt", label: "Descriere", widget: "string", default: "Camera Familie" }

      - name: "apt-predeal"
        label: "Apartament Predeal"
        file: "content/rooms/apt-predeal.json"
        fields:
          - { name: "name", label: "Nume cameră", widget: "string", default: "Apartament Predeal" }
          - { name: "price", label: "Preț per noapte (RON)", widget: "number", value_type: "int", min: 50, max: 5000, default: 480 }
          - name: "photos"
            label: "Poze (max 20)"
            widget: "list"
            max: 20
            summary: "{{fields.alt}}"
            fields:
              - { name: "src", label: "Imagine", widget: "image" }
              - { name: "alt", label: "Descriere", widget: "string", default: "Apartament Predeal" }

      - name: "apt-panoramic"
        label: "Apartament Panoramic"
        file: "content/rooms/apt-panoramic.json"
        fields:
          - { name: "name", label: "Nume cameră", widget: "string", default: "Apartament Panoramic" }
          - { name: "price", label: "Preț per noapte (RON)", widget: "number", value_type: "int", min: 50, max: 5000, default: 600 }
          - name: "photos"
            label: "Poze (max 20)"
            widget: "list"
            max: 20
            summary: "{{fields.alt}}"
            fields:
              - { name: "src", label: "Imagine", widget: "image" }
              - { name: "alt", label: "Descriere", widget: "string", default: "Apartament Panoramic" }

  - name: "gallery"
    label: "📸 Galerie Generală"
    files:
      - name: "gallery"
        label: "Poze galerie"
        file: "content/gallery.json"
        fields:
          - name: "photos"
            label: "Poze galerie (drag & drop pentru reordonare)"
            widget: "list"
            summary: "{{fields.alt}}"
            fields:
              - { name: "src", label: "Imagine", widget: "image" }
              - { name: "alt", label: "Descriere", widget: "string" }
              - { name: "category", label: "Categorie", widget: "select", options: ["exterior", "camere", "facilitati", "imprejurimi"], default: "exterior" }
